# 🌐 Streamlit 기반 YOLO ROI 사람 검출 시스템

## 🎯 개요

웹 브라우저에서 실행 가능한 **Streamlit 기반** YOLO ROI 사람 검출 시스템입니다.

## ✨ 주요 기능

### 🎨 웹 UI 기반
- **브라우저에서 실행** - 별도 프로그램 설치 불필요
- **직관적인 인터페이스** - 클릭만으로 모든 기능 사용
- **실시간 시각화** - 검출 결과를 실시간으로 확인
- **원격 접속 가능** - 네트워크를 통해 어디서든 접속

### 📐 ROI 편집 기능
- **좌표 입력으로 Polygon 그리기** - X, Y 좌표 입력
- **점 추가/삭제** - 각 꼭지점 개별 관리
- **ROI 관리** - 저장, 수정, 삭제 기능
- **실시간 미리보기** - 카메라 화면에 ROI 표시

### 🎥 실시간 검출
- **YOLO 기반 사람 검출**
- **실시간 Bounding Box 표시** - 검출된 사람마다 파란색 박스 + 신뢰도 점수 표시
- **ROI별 상태 추적** - ROI 내 사람 있음(초록색)/없음(빨간색) 표시
- **자동 API 이벤트 전송**
- **검출 통계 실시간 표시**

### 📊 대시보드
- **ROI별 상태 모니터링**
- **이벤트 로그 표시**
- **검출 통계 시각화**

## 🚀 빠른 시작

### 1. 패키지 설치

```bash
cd /home/user/yolo_roi_detector

# 필요한 패키지 설치
pip install streamlit ultralytics opencv-python requests numpy pillow

# 또는 requirements.txt 사용
pip install -r requirements.txt
```

### 2. Streamlit 앱 실행

```bash
# Streamlit 앱 실행
streamlit run streamlit_app.py

# 브라우저가 자동으로 열립니다
# 또는 http://localhost:8501 접속
```

### 3. 외부 접속 허용 (옵션)

```bash
# 네트워크의 다른 기기에서 접속 가능
streamlit run streamlit_app.py --server.address=0.0.0.0 --server.port=8501
```

## 📋 사용 방법

### 📐 탭 1: ROI 편집

#### ROI 그리기

1. **좌표 입력**
   - X 좌표, Y 좌표 입력
   - "➕ 점 추가" 버튼 클릭
   - 최소 3개 이상의 점 추가

2. **Polygon 저장**
   - ROI ID 입력 (예: "entrance", "corridor")
   - 설명 입력 (예: "출입구 영역")
   - "✅ Polygon 저장" 버튼 클릭

3. **점 관리**
   - 각 점 옆의 "🗑️" 버튼으로 삭제 가능
   - "🔄 현재 Polygon 초기화"로 전체 초기화

#### ROI 관리

- **저장된 ROI 확인**: 우측 패널에서 모든 ROI 확인
- **ROI 선택**: "🎯 선택" 버튼으로 선택 (노란색으로 강조)
- **ROI 삭제**: "🗑️ 삭제" 버튼으로 개별 삭제
- **전체 초기화**: "🧹 모든 ROI 초기화" 버튼

### 🎥 탭 2: 실시간 검출

1. **검출 시작**
   - "▶️ 검출 시작" 버튼 클릭
   - 카메라 화면에 실시간 검출 결과 표시

2. **검출 중지**
   - "⏸️ 검출 중지" 버튼 클릭

3. **실시간 상태 확인**
   - 우측 패널에서 각 ROI의 상태 확인
   - 🟢 present / 🔴 absent / ⚪ None
   - 검출 카운트 표시

### 📊 탭 3: 통계 & 로그

1. **검출 통계**
   - 각 ROI별 현재 상태
   - 총 검출 횟수

2. **이벤트 로그**
   - 최근 50개 이벤트 표시
   - 시간, ROI ID, 상태 정보
   - "🧹 로그 초기화" 버튼으로 삭제

## 🎨 실시간 시각화 기능

### 검출 화면 표시 요소

실시간 검출 화면에서는 다음과 같은 시각 요소가 표시됩니다:

#### 1️⃣ **사람 검출 Bounding Box**
- **색상:** 파란색 (RGB: 255, 0, 0)
- **두께:** 2픽셀
- **라벨:** `Person {confidence}` (예: "Person 0.95")
- **위치:** 박스 상단에 표시

#### 2️⃣ **ROI 영역 표시**
- **ROI 있음 (사람 검출):** 초록색 (RGB: 0, 255, 0)
  - 반투명 채우기 (투명도 20%)
  - 테두리 두께 2픽셀
- **ROI 없음 (사람 없음):** 빨간색 (RGB: 0, 0, 255)
  - 반투명 채우기 (투명도 20%)
  - 테두리 두께 2픽셀

#### 3️⃣ **ROI 정보 텍스트**
- **ROI ID:** 영역 중앙에 표시 (큰 글씨)
- **상태:** ROI ID 아래에 표시 (예: "present", "absent", "None")
- **카운트:** 상태 아래에 표시 (예: "Count: 15")

### 시각화 예시

```
┌─────────────────────────────────────┐
│  [카메라 화면]                       │
│                                     │
│  ┌─────────┐                        │
│  │ Person  │ ← 파란색 BBox          │
│  │  0.95   │                        │
│  └─────────┘                        │
│      ↓                              │
│  ┌────────────────┐                 │
│  │   ROI1 (초록)   │ ← 사람 있음     │
│  │   present       │                │
│  │   Count: 15     │                │
│  └────────────────┘                 │
│                                     │
│  ┌────────────────┐                 │
│  │   ROI2 (빨강)   │ ← 사람 없음     │
│  │   absent        │                │
│  │   Count: 0      │                │
│  └────────────────┘                 │
└─────────────────────────────────────┘
```

## ⚙️ 사이드바 설정

### 🤖 YOLO 모델

- **yolov8n.pt**: 가장 빠름 (권장)
- **yolov8s.pt**: 빠르고 정확
- **yolov8m.pt**: 균형잡힌 성능
- **yolov8l.pt**: 높은 정확도

### 📹 카메라

- **웹캠**: 웹캠 번호 선택 (0, 1, 2...)
- **비디오 파일**: 파일 경로 입력

### 🎯 검출 설정

- **신뢰도 임계값**: 0.0 ~ 1.0 (기본: 0.5)
- **존재 확인 시간**: 초 단위 (기본: 5초)
- **부재 확인 시간**: 초 단위 (기본: 3초)

### 🌐 API 설정

- **API 엔드포인트**: API URL 입력
- **Watch ID**: 고유 식별자

### 💾 설정 저장

- "💾 설정 저장" 버튼 클릭
- config.json 파일에 저장됨

## 🎨 화면 구성

```
┌─────────────────────────────────────────────────────┐
│  ⚙️ 사이드바 (설정)                                 │
│  - YOLO 모델 선택                                   │
│  - 카메라 설정                                      │
│  - 검출 임계값                                      │
│  - API 설정                                         │
│  - 설정 저장 버튼                                   │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  📐 탭 1: ROI 편집                                  │
├───────────────────────┬─────────────────────────────┤
│  🎨 ROI 그리기         │  🛠️ 편집 도구              │
│  [카메라 화면]         │  - 현재 점 개수            │
│  [좌표 입력]           │  - 점 목록 & 삭제          │
│  X: [___] Y: [___]    │  - ROI ID/설명 입력        │
│  [➕ 점 추가]          │  - Polygon 저장            │
│                       │  - 저장된 ROI 목록         │
└───────────────────────┴─────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  🎥 탭 2: 실시간 검출                               │
├───────────────────────┬─────────────────────────────┤
│  [▶️ 검출 시작]        │  📊 실시간 상태             │
│  [검출 화면]           │  ROI1: 🟢 present          │
│  - 파란색 BBox 표시    │  Count: 15                  │
│  - 신뢰도 점수 표시    │  ROI2: 🔴 absent           │
│  - ROI 영역 색상 표시  │  Count: 0                   │
│    (초록=존재,빨강=부재)│                            │
└───────────────────────┴─────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  📊 탭 3: 통계 & 로그                               │
├───────────────────────┬─────────────────────────────┤
│  📊 검출 통계          │  📝 이벤트 로그             │
│  ROI1: present        │  🟢 [14:30:25] ROI1: 1     │
│  카운트: 15           │  🔴 [14:28:10] ROI2: 0     │
│                       │  [🧹 로그 초기화]           │
└───────────────────────┴─────────────────────────────┘
```

## 🔧 고급 기능

### 원격 접속 설정

```bash
# 모든 네트워크 인터페이스에서 접속 허용
streamlit run streamlit_app.py \
  --server.address=0.0.0.0 \
  --server.port=8501 \
  --server.headless=true
```

### 백그라운드 실행

```bash
# nohup으로 백그라운드 실행
nohup streamlit run streamlit_app.py > streamlit.log 2>&1 &

# 프로세스 확인
ps aux | grep streamlit

# 종료
pkill -f streamlit
```

### 포트 변경

```bash
# 다른 포트 사용
streamlit run streamlit_app.py --server.port=8080
```

## 📊 API 이벤트 형식

```json
{
  "eventId": "uuid",
  "roiId": "ROI1",
  "objectType": "human",
  "status": 1,
  "createdAt": "2025-12-01T14:30:00",
  "watchId": "watch_streamlit"
}
```

## 🎯 장점

### Streamlit UI의 장점

| 항목 | OpenCV 버전 | Streamlit 버전 |
|------|------------|----------------|
| **접근성** | 로컬 실행만 가능 | 웹 브라우저 접속 |
| **원격 접속** | ❌ 불가능 | ✅ 가능 |
| **UI** | 키보드 단축키 | 버튼, 슬라이더 |
| **설정 변경** | 파일 수정 필요 | UI에서 즉시 변경 |
| **모니터링** | 화면만 | 통계, 로그, 그래프 |
| **사용 편의성** | 보통 | 매우 높음 |

## 🔍 문제 해결

### Q: Streamlit이 실행되지 않아요

**A**: Streamlit 설치 확인
```bash
pip install streamlit
streamlit --version
```

### Q: 카메라가 열리지 않아요

**A**: 카메라 번호 확인
- 사이드바에서 웹캠 번호 변경 (0, 1, 2...)
- 다른 프로그램이 카메라를 사용 중인지 확인

### Q: 외부에서 접속이 안돼요

**A**: 방화벽 설정 확인
```bash
# 포트 8501 허용 필요
sudo ufw allow 8501
```

### Q: ROI 편집이 어려워요

**A**: 좌표 확인 방법
- 카메라 화면의 크기 확인 (예: 1280x720)
- 화면 안의 좌표만 입력
- 이미지 편집 프로그램으로 좌표 미리 확인

## 📱 모바일 지원

Streamlit은 반응형 웹 디자인을 지원합니다:
- 스마트폰 브라우저에서도 접속 가능
- 태블릿에서 모니터링 가능

## 🚀 프로덕션 배포

### Docker 사용 (권장)

```dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

EXPOSE 8501

CMD ["streamlit", "run", "streamlit_app.py", \
     "--server.address=0.0.0.0", \
     "--server.port=8501"]
```

### systemd 서비스

```ini
[Unit]
Description=YOLO ROI Detector Streamlit
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/yolo_roi_detector
ExecStart=/usr/local/bin/streamlit run streamlit_app.py
Restart=always

[Install]
WantedBy=multi-user.target
```

## 📝 주의사항

1. **YOLO 모델**: 첫 실행 시 자동 다운로드 (시간 소요)
2. **카메라 권한**: 웹캠 사용 권한 필요
3. **네트워크**: 외부 접속 시 보안 설정 필요
4. **성능**: 실시간 검출은 GPU 권장

## 🔗 관련 문서

- [기본 README](README.md)
- [Polygon ROI 가이드](README_POLYGON.md)
- [GitHub 저장소](https://github.com/futurianh1k/roidetyolo)

## 📧 지원

문제가 발생하면 GitHub 이슈로 문의해주세요!

---

**Streamlit 버전으로 더 편리하게 YOLO ROI 검출을 사용하세요!** 🎉
